# Paddle (aktif etmek için doldur)
NEXT_PUBLIC_PADDLE_ENV=production
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=pk_live_xxx

# (ileride) Webhook için
PADDLE_WEBHOOK_SECRET=whsec_xxx

# (opsiyonel) App URL
NEXT_PUBLIC_APP_URL=https://vibraxx.com
