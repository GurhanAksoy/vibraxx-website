export const metadata = { title: "ShortAge / LongAge" };
export default function RootLayout({ children }) {
  return <html lang="tr"><body style={{margin:0,fontFamily:"ui-sans-serif"}}>{children}</body></html>;
}
